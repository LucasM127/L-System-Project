//LSFileParser parses the lsfile roughly
//creates the rawstring data for each production, separated by the 'tokens'
//Loader does the validation work
//paramnumMap generation/validation
//uses global map, define map, etc...
//generates the eval strings -> tokenized & validated eval strings ready for rpn algorithm generator